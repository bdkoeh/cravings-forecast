services:
  sf-cravings-forecast:
    build: .
    container_name: sf-cravings-forecast
    restart: unless-stopped
    env_file:
      - .app.env
    environment:
      NODE_ENV: production
      PORT: 3001
    ports:
      - "127.0.0.1:8083:3001"
    volumes:
      - ./data:/app/data
